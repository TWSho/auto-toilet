.pio
.cache
.dummy
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
compile_commands.json

# frontend build tooling (data/web is the built output actually flashed via
# uploadfs and stays tracked - see web/package.json's `deploy` script)
web/node_modules
web/dist
