# WebUIフロントエンドへの追加依頼

参照: [WebUIバックエンド仕様.md](./WebUIバックエンド仕様.md)（本依頼内のAPI詳細はすべてこちらが正）

## 依頼文言（そのまま貼り付け可）

> バックエンド（M5StampS3）側のAPI仕様を確定させました。現行のフロントエンド仕様書はデモ実装（React state完結、実通信なし）を前提にしていますが、実機と通信する前提で以下A〜Dの追加・修正をお願いします。
>
> - **A. 状態表示の追加**: 在室状態・通信状態・未実装コマンド/クールダウンのフィードバックなど、実通信によって新たに必要になった表示
> - **B. 設定パネルの新規追加**: 人感センサーの感度／検知距離／滞在継続時間を調整するUI（現状どのカードにも存在しない）
> - **C. 既存文言の修正**: 「デモのため反映されません」等、実装により事実と異なくなった注記の削除
> - **D. API対応表**: 各カードの操作から呼ぶべきエンドポイントの一覧
>
> API仕様は `WebUIバックエンド仕様.md` を参照してください。ポーリング間隔は `GET /api/status` を1000ms間隔で呼ぶ想定です。

---

## A. 状態表示の追加

### A-1. 人感センサーの在室状態
- 表示場所: トイレカード推奨（現状どこにも表示がない）
- データ: `GET /api/status` の `roomState`（`"VACANT" | "ENTERING" | "STAYING"`）
- 表示例: 「不在」「入室中」「滞在中」

### A-2. 通信状態インジケータ
- データ: `GET /api/status` の `wifi.connected` / `mqtt.connected` / `wled.reachable`
- ポーリングが一定回数連続失敗した場合（マイコン自体に届かない）は「マイコンと通信できません」等のオフライン表示に切り替える

### A-3. 未実装コマンドのハンドリング
- `GET /api/toilet/commands` の各コマンドの `implemented: boolean` を見て、`false`のボタンはグレーアウトまたは無効化する
- 実行時に `501` が返った場合も同様に「未対応」であることをフィードバック（トースト等）

### A-4. クールダウン中の表示
- `POST /api/toilet/commands/{id}/execute` が `409`（`{"error":{"code":"cooldown", ...}}`）を返した場合、対象ボタンを一時的に無効化するか「少し待ってから操作してください」を表示

---

## B. 設定パネルの新規追加

現状の4カード（トイレ／LED・BGM／シーン）とは別に、人感センサーの調整用パネル（別画面・モーダルなど）を追加してください。

| フィールド | 範囲 | 刻み |
|---|---|---|
| sensitivity（感度） | 0-100 | 10 |
| maxGate（検知距離/ゲート） | 1-8 | 1 |
| stayDurationSec（滞在継続時間・秒） | 5-60 | 5 |

- 取得: `GET /api/settings` → `{ current: {...}, saved: {...}, dirty: boolean }`
- ライブ変更: `PUT /api/settings`（変更したいフィールドのみ送信）→ 即座にセンサーへ反映（NVS未保存）
- 確定保存: `POST /api/settings/save`（ボディなし）→ NVSへ保存、以後再起動しても保持
- UX: `dirty: true` の間は「未保存の変更があります」等を表示し、保存ボタンを目立たせる（他カードのような即時反映オンリーではなく、確定操作が必要な点に注意）

---

## C. 既存文言の修正

| 箇所 | 現在の文言 | 修正内容 |
|---|---|---|
| LEDカスタムカラー | 「デモのため実機への反映はありません」 | 削除（実際にWLEDへ反映される） |
| シーン新規作成の保存ボタン | 「保存（デモ）」 | 「保存」に変更（LittleFSに永続化されるためリロードしても消えない） |
| 6章「現状の制約・未実装事項」冒頭 | 「マイコンとの実通信なし。全操作はブラウザ内のReact stateのみで完結」 | 削除し、実際に残る制約に置き換える（下記D参照、および乾燥・フタ自動開閉は引き続き未実装） |

---

## D. API対応表（カード別）

### トイレカード
| UI操作 | Method/Path |
|---|---|
| 流す(大/小/ECO)、スプレー各種、水勢、洗浄位置、便座/温水温度、脱臭、ノズルそうじ、オート洗浄 | `POST /api/toilet/commands/{id}/execute`（idはコマンドID、`WebUIバックエンド仕様.md`のコマンド表参照） |
| 各操作の現在値表示 | `GET /api/toilet/state` |
| 使用可能なコマンド一覧・実装状況 | `GET /api/toilet/commands` |

### LEDモジュール
| UI操作 | Method/Path |
|---|---|
| 明るさ・プリセット色・カスタムカラー変更 | `PUT /api/led` |
| 現在の状態取得 | `GET /api/led` |

### BGMモジュール
| UI操作 | Method/Path |
|---|---|
| 再生/一時停止、音量、選曲、シーク、リピート、シャッフル | `PUT /api/bgm` |
| 曲送り/曲戻し | `POST /api/bgm/next` / `POST /api/bgm/previous` |
| 現在の再生状態取得 | `GET /api/bgm` |
| 曲一覧 | `GET /api/bgm/tracks` |

### シーンカード
| UI操作 | Method/Path |
|---|---|
| シーン一覧表示 | `GET /api/scenes` |
| シーン実行（LED+BGM一括変更） | `POST /api/scenes/{id}/execute` |
| 新規シーン作成 | `POST /api/scenes` |

### 新規: 設定パネル（B参照）
| UI操作 | Method/Path |
|---|---|
| 設定値取得 | `GET /api/settings` |
| ライブ変更 | `PUT /api/settings` |
| 確定保存 | `POST /api/settings/save` |

### 全カード共通
| UI操作 | Method/Path |
|---|---|
| ポーリング（状態表示全般） | `GET /api/status`（推奨1000ms間隔） |
